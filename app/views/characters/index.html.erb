<%= flash[:message] %>
<%= display_header %><br>
<fieldset>
   <% @characters.each do |c| %>
    Name:  <%=link_to(c.name.capitalize,  "characters/#{c.id}") %><br />
    Level: <%= c.level %><br />
    Race: <%= c.race %><br />
    Class: <%= c.character_class %><br /><br />
    <hr>
<% end %>
</fieldset>
